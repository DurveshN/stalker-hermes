{
  "name": "stalker-hermes",
  "version": "0.1.0",
  "private": true,
  "description": "Stalker Hermes — an AI agency that tracks competitors across LinkedIn, X, news, blogs and SEO, files GitHub issues/PRs, and briefs you in Slack. Python crew on Hermes, powered by Linkup + Convex + Cloudflare + ElevenLabs + Dodo.",
  "workspaces": [
    "landing",
    "workers",
    "convex-backend"
  ],
  "scripts": {
    "convex:dev": "npm --workspace convex-backend run dev",
    "convex:deploy": "npm --workspace convex-backend run deploy",
    "landing:dev": "npm --workspace landing run dev",
    "landing:deploy": "npm --workspace landing run deploy",
    "worker:deploy": "npm --workspace workers run deploy"
  },
  "engines": {
    "node": ">=20"
  }
}
